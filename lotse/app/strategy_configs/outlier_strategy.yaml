metadata:
  strategy: Outlier Detection
  description: Detects outliers and guides towards them
  strategy_id: outlier_detection
  degree: orienting

action:
  file_path: actions/outlier_action.yaml

determine_applicability:
  args: [ctx, delta]
  load: return True
